export const { DADATA_API_KEY } = process.env;

export const { RESEND_API_KEY } = process.env;

export const API_URL = process.env.NEXT_PUBLIC_API_URL;

export const { NEXTAUTH_SECRET } = process.env;

export const { YOOKASSA_API_KEY } = process.env;
export const { YOOKASSA_CALLBACK_URL } = process.env;
export const { YOOKASSA_STORE_ID } = process.env;

export const { GITHUB_ID } = process.env;
export const { GITHUB_SECRET } = process.env;

export const { GOOGLE_ID } = process.env;
export const { GOOGLE_SECRET } = process.env;

export const BASE_URL = process.env.NEXT_PUBLIC_BASE_URL;
